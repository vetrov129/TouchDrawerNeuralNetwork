package hi.dude.touchdrawer.enums

import android.graphics.Color

enum class PColor(val color: Int) {
    RED(Color.RED),
    GREEN(Color.GREEN),
    BLUE(Color.BLUE),
    YELLOW(Color.YELLOW),
    CYAN(Color.CYAN),
    MAGENTA(Color.MAGENTA),
    WHITE(Color.WHITE),
    BLACK(Color.BLACK)
}